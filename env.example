## Database (Supabase)
# Recommended: use a separate schema to avoid clobbering public:
# - DATABASE_URL: pooled / transaction pooler URL (serverless runtime)
# - DIRECT_URL: direct connection URL (migrations)
#
# You can also append `?schema=baseline` to both URLs to keep tables isolated.
DATABASE_URL=""
DIRECT_URL=""

## Auth (NextAuth + Google OAuth)
NEXTAUTH_URL="https://baseline-video.vercel.app"
NEXTAUTH_SECRET=""
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

## Public base (optional)
NEXT_PUBLIC_BASE_URL="https://baseline-video.vercel.app"


